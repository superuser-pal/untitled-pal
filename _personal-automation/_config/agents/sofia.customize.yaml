# Agent Customization: Sofia (Personal Finance Advisor)
# Customize any section below - all are optional

# Override agent name
agent:
  metadata:
    name: ""

# Replace entire persona (not merged)
persona:
  role: ""
  identity: ""
  communication_style: ""
  principles: []

# Add custom critical actions (appended after standard config loading)
critical_actions: []

# Add persistent memories for the agent
memories: []
# Example:
# memories:
#   - "User's primary financial goal is saving for a house"
#   - "User prefers weekly spending reports"
#   - "User has subscription tracking enabled"

# Add custom menu items (appended to base menu)
# Don't include * prefix or help/exit - auto-injected
menu: []
# Example:
# menu:
#   - cmd: custom-audit
#     workflow: "{project-root}/_personal-automation/domains/finance/workflows/my-audit.yaml"
#     description: Custom financial audit workflow

# Add custom prompts (for action="#id" handlers)
prompts: []
# Example:
# prompts:
# - id: custom-report
#   content: |
#     Generate a custom financial report with specific formatting
