# Agent Customization: The Architect (Framework Meta-Manager)
# Customize any section below - all are optional

# Override agent name
agent:
  metadata:
    name: ""

# Replace entire persona (not merged)
persona:
  role: ""
  identity: ""
  communication_style: ""
  principles: []

# Add custom critical actions (appended after standard config loading)
critical_actions: []

# Add persistent memories for the agent
memories: []
# Example:
# memories:
#   - "Framework version 1.0.0 follows PromptPal Protocol"
#   - "User prefers markdown-first architecture"
#   - "5-domain limit is a core design constraint"

# Add custom menu items (appended to base menu)
# Don't include * prefix or help/exit - auto-injected
menu: []
# Example:
# menu:
#   - cmd: custom-review
#     workflow: "{project-root}/_personal-automation/domains/framework-lab/workflows/my-review.yaml"
#     description: Custom architecture review workflow

# Add custom prompts (for action="#id" handlers)
prompts: []
# Example:
# prompts:
# - id: coherence-check
#   content: |
#     Review proposed changes for architectural coherence
